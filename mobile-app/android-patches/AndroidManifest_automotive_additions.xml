<?xml version="1.0" encoding="utf-8"?>
<!--
  AndroidManifest_automotive_additions.xml
  Fragments that must be merged into:
    mobile-app/android/app/src/main/AndroidManifest.xml
  after `npx expo prebuild`.

  INSTRUCTIONS:
    1. Open the generated AndroidManifest.xml.
    2. Add the <uses-permission> elements inside <manifest>.
    3. Add the <uses-feature> elements inside <manifest>.
    4. Add the <meta-data> element inside <application>.
    5. Add the <service> element inside <application>.
-->

<!-- ── Paste inside <manifest> ─────────────────────────────────────────── -->
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.RECORD_AUDIO" />
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

<uses-feature
    android:name="androidx.car.app.MAP_TEMPLATES"
    android:required="false" />
<uses-feature
    android:name="androidx.car.app.NAVIGATION_TEMPLATES"
    android:required="false" />

<!-- ── Paste inside <application> ──────────────────────────────────────── -->

<!-- Android Auto automotive app descriptor reference -->
<meta-data
    android:name="com.google.android.gms.car.application"
    android:resource="@xml/automotive_app_desc" />

<!-- Iron Eagle Car Service — Android Auto entry point -->
<service
    android:name=".IronEagleCarService"
    android:exported="true">
    <intent-filter>
        <action android:name="androidx.car.app.CarAppService" />
        <category android:name="androidx.car.app.category.IOT" />
    </intent-filter>
</service>

# Inference Service Configuration

# Server
PORT=3000
NODE_ENV=production

# Security — comma-separated list of allowed CORS origins
ALLOWED_ORIGINS=https://xbfnlzmpumthnjmtqufp.supabase.co

# Model paths (defaults shown — override if models are elsewhere)
YOLO_MODEL_PATH=./models/yolov8n.onnx
EMBEDDING_MODEL_PATH=./models/mobilenet_v3.onnx

# Vehicle detection confidence threshold (0.0–1.0)
DETECTION_CONFIDENCE=0.5

# OpenAI Vision — OPTIONAL
# When set, each /infer call sends the photo to gpt-4o-mini to extract:
#   plate number, vehicle make/model/colour, self-contained flag
# Without this key the service still returns the YOLO detection + 384-D
# embedding; the edge function (orc-ingest) will fall back to accepting
# client-side plate data from OnSpace AI in that case.
OPENAI_API_KEY=

# Performance
MAX_UPLOAD_SIZE=10485760  # 10MB in bytes
REQUEST_TIMEOUT=30000     # 30 seconds

# Monitoring (optional)
SENTRY_DSN=
LOG_LEVEL=info

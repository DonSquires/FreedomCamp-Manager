# Inference Service Configuration

# Server
PORT=3000
NODE_ENV=production

# Security — comma-separated list of allowed CORS origins
ALLOWED_ORIGINS=https://xbfnlzmpumthnjmtqufp.supabase.co

# Model paths (defaults shown — override if models are elsewhere)
YOLO_MODEL_PATH=./models/yolov8n.onnx
EMBEDDING_MODEL_PATH=./models/mobilenet_v3.onnx

# Vehicle detection confidence threshold (0.0–1.0)
DETECTION_CONFIDENCE=0.5

# OpenAI Vision — OPTIONAL (Railway service only)
# When set, each /infer call uses gpt-4o-mini to extract:
#   plate number, vehicle make/model/colour, self-contained flag
# NOTE: Plate Recognizer API (set in Supabase secrets) is the PRIMARY
# plate recognition source — OpenAI Vision on Railway is only a backup
# for the embedding pipeline when Plate Recognizer is not configured.
OPENAI_API_KEY=

# Performance
MAX_UPLOAD_SIZE=10485760  # 10MB in bytes
REQUEST_TIMEOUT=30000     # 30 seconds

# Monitoring (optional)
SENTRY_DSN=
LOG_LEVEL=info

# ─────────────────────────────────────────────────────────────────────────────
# PLATE RECOGNIZER — these secrets are already set in Supabase
# ─────────────────────────────────────────────────────────────────────────────
# The following secrets are already configured in your Supabase project.
# This Railway service does NOT use them directly — orc-ingest/vehicle-ingest
# Edge Functions read them from Supabase secrets at runtime.
#
# Already set in Supabase:
#   PLATERECOGNIZER_TOKEN   ← primary token (canonical name used by code)
#   ALPR_API_TOKEN          ← same value, alternate accepted name
#   ALPR_API_URL            ← API endpoint (https://api.platerecognizer.com/v1/plate-reader/)
#   PARKPOW_API_TOKEN       ← ParkPow enforcement integration
#
# To rotate or update:
#   supabase secrets set PLATERECOGNIZER_TOKEN="new-token"
#   supabase secrets set ALPR_API_URL="https://api.platerecognizer.com/v1/plate-reader/"

// Add this to your Edge Function headers
export const corsHeaders = {
  'Access-Control-Allow-Origin': '*',
  'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type, x-client-timezone', // <--- MUST include this
  'Access-Control-Allow-Methods': 'POST, OPTIONS',
}

// Ensure your serve function handles OPTIONS requests first
serve(async (req) => {
  if (req.method === 'OPTIONS') {
    return new Response('ok', { headers: corsHeaders })
  }
  
  // Your vehicle-ingest logic follows...
})

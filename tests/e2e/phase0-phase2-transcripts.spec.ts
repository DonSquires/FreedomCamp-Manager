import { test } from '@playwright/test'

// Phase 0-2 placeholder: implemented in follow-up tickets.
test.describe('Phase 0-2 Streaming STT + Live Captions', () => {
  test.skip(true, 'Scaffold only: implement transcript latency and org isolation assertions in Phase 0-2.')

  test('transcript ingestion contract', async () => {
    // Intentionally empty while transcript persistence is being wired.
  })
})
